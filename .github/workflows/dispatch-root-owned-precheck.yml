name: Dispatch root-owned pre-check

on:
  workflow_dispatch:

jobs:
  call-precheck:
    runs-on: ubuntu-latest
    steps:
      - name: Call reusable root-owned pre-check
        uses: ./.github/workflows/root-owned-precheck.yml
        with:
          runner: '["self-hosted"]'
